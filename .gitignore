/vendor/
node_modules/
npm-debug.log
yarn-error.log

# Laravel 4 specific
bootstrap/compiled.php
app/storage/

# Laravel 5 & Lumen specific
public/storage
public/hot
public/build

# Laravel 5 & Lumen specific with changed public path
public_html/storage
public_html/hot

storage/*.key
.env
.env.local
.env.testing.local

Homestead.yaml
Homestead.json
/.vagrant
.phpunit.result.cache
rr
.rr.yaml

/storage/rector

/.idea

# Agent worktree checkouts. The committed agent configuration is AGENTS.md,
# CLAUDE.md and docs/agents/ — this directory is scratch.
/.claude/

# PHP's default error log lands here under some server configurations. It was
# committed once, leaking the deployment path in every line.
/error_log
